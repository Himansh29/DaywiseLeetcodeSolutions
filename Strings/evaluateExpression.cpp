#include<bits/stdc++.h>
using namespace std;
int main()
{
    string s = "2 + 3";

    stack<char> numbers;
    stack<char> op;
    int i = 0;

    while(i < s.length()) {
        if(s[i] )
    }

    return 0;
}
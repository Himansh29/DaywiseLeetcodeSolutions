#include<bits/stdc++.h>
using namespace std;
void solve(vector<int> a, vector<vector<int>> ans, vector<int> op, int i){

    if()
}
vector<vector<int>> subsets(vector<int> a){
    vector<vector<int>> ans;
    vector<int> op;
    solve(a,ans,op,0);
}
int main()
{
    vector<int> a = {1,2,3};

    return 0;
}